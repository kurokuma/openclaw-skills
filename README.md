# openclaw-skills
require
- `pip install censys-platform`
- `pip install vt-py`
- `sudo apt install -y chromium-chromedriver`
- `pip install selenium`

set `~/.openclaw/api_config.json`

```
{
    "api_token": {
        "virustotal-search": {
            "vt_token": ""
        },
        "censys-search": {
            "pat": ""
        }
    }
}
```
